# Backend Configuration
PORT=8080

# Google Cloud Platform (Required)
# Create a project at https://console.cloud.google.com
# Create a Service Account Key if running locally, or use Application Default Credentials.
GCP_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
GCS_BUCKET_NAME=your-bucket-name

# LLM Configuration (Required)
# Get key from https://aistudio.google.com/app/apikey (Gemini) or https://console.groq.com (Groq)
GEMINI_API_KEY=your-gemini-key
# GROQ_API_KEY=your-groq-key (Optional if provider is groq)

# Provider Settings
LLM_PROVIDER=gemini
CHAT_MODEL=gemini-3-flash-preview
PARSE_MODEL=gemini-3-pro-preview

# Security
# 12 hours = 43200 seconds
SIGNED_URL_TTL_SECONDS=43200
SYSTEM_INSTRUCTION="你是一位專業的國外部資深主管助手，負責解答員工關於公司國外部標準作業程序 (SOP) 的問題。"
